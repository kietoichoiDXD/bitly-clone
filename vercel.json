{
    "version": 2,
    "framework": "laravel",
    "buildCommand": "composer install --no-dev && npm install && npm run build && php artisan route:cache && php artisan config:cache && php artisan view:cache",
    "installCommand": "composer install --no-dev",
    "outputDirectory": "public",
    "functions": {
        "api/*.php": {
            "runtime": "vercel-php@0.7.0"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.php"
        }
    ]
}
